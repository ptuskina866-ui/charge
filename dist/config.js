// Set a real HTTPS endpoint accepting JSON and returning { "ok": true } only after durable receipt.
// No contacts are transmitted or stored until an endpoint is configured.
window.TESCHEV_CONFIG = Object.freeze({ leadEndpoint: '' });
